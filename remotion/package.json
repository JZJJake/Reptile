{
  "name": "reptile-remotion",
  "version": "1.0.0",
  "description": "Motion source-of-truth for Reptile's brand identity — the self-weaving knowledge constellation. Rendered to video or used as the spec for the dependency-free CSS/SVG port shipped in static/login.html.",
  "private": true,
  "scripts": {
    "dev": "remotion studio",
    "render": "remotion render KnowledgeConstellation out/constellation.mp4",
    "render:loop": "remotion render KnowledgeConstellation out/constellation.mp4 --frames=0-239",
    "upgrade": "remotion upgrade"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.4.0"
  }
}
